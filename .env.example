# Cloudinary — image optimisation & delivery
# Cloud name is public and used in frontend URLs
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name

# API key and secret are server-side only (used by the upload script)
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
